{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    },
    "WriteTo": [
      { "Name": "Console" }
    ],
    "Enrich": [ "FromLogContext" ]
  },
  "AllowedHosts": "*",

  "Auth": {
    "//": "Mode = Local (this API authenticates against the member DB and issues its own tokens) or PassThrough (BFA: relay to the on-prem ARTS auth service via Apigee Internal).",
    "Mode": "Local",
    "MaxRequestBodyBytes": 32768
  },

  "ApigeeInternal": {
    "//": "Only used when Auth:Mode = PassThrough. ApiKey and ClientCertificatePassword must come from Key Vault / OpenShift secrets, never from this file.",
    "BaseAddress": "",
    "LoginPath": "auth/login",
    "RefreshPath": "auth/refresh",
    "TimeoutSeconds": 15,
    "ApiKeyHeaderName": "x-api-key",
    "ApiKey": "",
    "ClientCertificatePath": "",
    "ClientCertificatePassword": "",
    "MaxRetryAttempts": 2,
    "RetryBaseDelayMilliseconds": 200,
    "MaxResponseBodyBytes": 262144,
    "CorrelationIdHeaderName": "X-Correlation-Id",
    "ForwardRequestHeaders": [ "Accept-Language", "X-Correlation-Id", "X-Request-Id", "X-Device-Id", "X-Channel", "User-Agent" ],
    "ForwardResponseHeaders": [ "WWW-Authenticate", "Retry-After", "X-Correlation-Id", "X-Request-Id", "X-RateLimit-Limit", "X-RateLimit-Remaining", "X-RateLimit-Reset" ]
  },

  "Database": {
    "//": "Provider = InMemory (mock, default) or SqlServer (real existing member DB).",
    "Provider": "InMemory",
    "SeedMockData": true
  },

  "ConnectionStrings": {
    "//": "AuthDb holds the auth-owned schema (auth.RefreshTokens). Required when Database:Provider = SqlServer.",
    "AuthDb": ""
  },

  "LobConnections": {
    "//": "One connection string per LOB. Used by the Dapper repository to pick the member DB at login. Supply from Key Vault/secrets in production.",
    "Connections": {
      "DENTAL": "",
      "VISION": "",
      "MEDICAL": "",
      "RX": ""
    }
  },

  "Jwt": {
    "Issuer": "https://auth.memberportal.local",
    "Audience": "member-portal-apis",
    "AccessTokenMinutes": 15,
    "RefreshTokenDays": 7,
    "//PrivateKeyPem": "In production, inject a PKCS#8 RSA private key via secrets/Key Vault.",
    "PrivateKeyPath": "",
    "KeyDirectory": "keys"
  },

  "AccountLockout": {
    "MaxFailedAttempts": 5,
    "LockoutMinutes": 15
  },

  "PasswordHashing": {
    "//": "Match these to the EXACT scheme used by the existing database when you switch to SqlServer.",
    "Algorithm": "Sha512",
    "SaltPlacement": "Prefix",
    "HashEncoding": "Base64",
    "SaltEncoding": "Base64",
    "Iterations": 1,
    "SaltSizeBytes": 16
  },

  "Cors": {
    "AllowedOrigins": []
  }
}
