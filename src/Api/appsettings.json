{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    },
    "WriteTo": [
      { "Name": "Console" }
    ],
    "Enrich": [ "FromLogContext" ]
  },
  "AllowedHosts": "*",

  "Database": {
    "//": "Provider = InMemory (mock, default) or SqlServer (real existing member DB).",
    "Provider": "InMemory",
    "SeedMockData": true
  },

  "ConnectionStrings": {
    "//": "AuthDb holds the auth-owned schema (auth.RefreshTokens). MemberDb hosts the SSO config proc (MEM_SP_SSO_Config). Both required when Database:Provider = SqlServer.",
    "AuthDb": "",
    "MemberDb": ""
  },

  "LobConnections": {
    "//": "One connection string per LOB. Used by the Dapper repository to pick the member DB at login. Supply from Key Vault/secrets in production.",
    "Connections": {
      "DENTAL": "",
      "VISION": "",
      "MEDICAL": "",
      "RX": ""
    }
  },

  "Jwt": {
    "Issuer": "https://auth.memberportal.local",
    "Audience": "member-portal-apis",
    "AccessTokenMinutes": 15,
    "RefreshTokenDays": 7,
    "//PrivateKeyPem": "In production, inject a PKCS#8 RSA private key via secrets/Key Vault.",
    "PrivateKeyPath": "",
    "KeyDirectory": "keys"
  },

  "AccountLockout": {
    "MaxFailedAttempts": 5,
    "LockoutMinutes": 15
  },

  "PasswordHashing": {
    "//": "Match these to the EXACT scheme used by the existing database when you switch to SqlServer.",
    "Algorithm": "Sha512",
    "SaltPlacement": "Prefix",
    "HashEncoding": "Base64",
    "SaltEncoding": "Base64",
    "Iterations": 1,
    "SaltSizeBytes": 16
  },

  "Cors": {
    "AllowedOrigins": []
  },

  "Sso": {
    "//": "SSO feature (GET /api/v1/sso). SkipLobs = LOBs that get configuration back but no PingFederate hand-off (legacy SkipSSO:Lobs). HraAssessment replaces the legacy hard-coded LOB 2100 HRA minor/adult rule (legacy SSOAssessmentSettings).",
    "SkipLobs": [],
    "ConfigCacheSlidingExpiration": "1.00:00:00",
    "HraAssessment": {
      "Enabled": true,
      "Lob": "2100",
      "SsoName": "HRA",
      "MinorAgeLimit": 18,
      "MinorAssessmentName": "MinorAssessment",
      "AdultAssessmentName": "AdultAssessment"
    }
  }
}
