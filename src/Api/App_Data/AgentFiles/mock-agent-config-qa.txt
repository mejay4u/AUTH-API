# Mock PingFederate OpenToken agent configuration, used only by the InMemory provider's
# MockSsoConfigurationRepository so GET /api/v1/sso returns a complete sign-on URL without the
# real member database. Real environments deploy the per-connection agent-config files downloaded
# from the PingFederate admin console (e.g. hra-agent-config-qa.txt) into this directory —
# those contain real shared secrets and must NEVER be committed.
token-name=JivaZeomegaOpenToken
cipher-suite=2
password=bW9jay1vcGVudG9rZW4tc2hhcmVkLXNlY3JldA==
use-cookie=false
token-lifetime=300
renew-until=43200
not-before-tolerance=0
