{
  "DetailedErrors": true,

  "Database": {
    "//": "Development uses the EF Core InMemory provider so the flow runs without the real DB.",
    "Provider": "InMemory"
  },

  "Descope": {
    "//": "Skips token validation so requests.http works without a real Descope session. NEVER outside Development.",
    "AllowAnonymous": true
  }
}
