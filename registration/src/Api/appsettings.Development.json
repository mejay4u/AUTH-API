{
  "DetailedErrors": true,

  "Database": {
    "//": "Development uses the EF Core InMemory provider so the flow runs without the real DB.",
    "Provider": "InMemory"
  },

  "Facets": {
    "//": "Stub matches every lookup (except an SSN ending 0000) so the flow can be walked without Facets. Rejected outside Development.",
    "Provider": "Stub"
  },

  "ConnectorAuth": {
    "//": "A fixed development key. Send it as X-Connector-Key from the flow connector or requests.http.",
    "Keys": [ "dev-connector-key" ]
  }
}
