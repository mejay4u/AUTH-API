{
  "DetailedErrors": true,

  "Database": {
    "//": "Development uses the EF Core InMemory provider so the flow runs without the real DB.",
    "Provider": "InMemory"
  },

  "Descope": {
    "//": "Skip signature verification locally so the /descope endpoints can be called from Swagger.",
    "Enabled": false
  },

  "LegacyVerifyApi": {
    "//": "Use the in-process mock verifier (accepts legacy.user@example.com / Legacy#123).",
    "UseMock": true
  }
}
